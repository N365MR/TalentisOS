# Testing Strategy

Run `npm run build`, `npm test`, `node --test`, and `npm run check` for the foundation. Validate source scope with focused tests and inspect the app at 390px, 820px, and 1440px. Test future migrations before release to ensure non-destructive evolution.
