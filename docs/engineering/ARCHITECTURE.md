# Architecture

The application is Vite-built Vanilla JavaScript using browser-native ES modules, semantic HTML, and modern CSS. No UI framework is used. `src/main.js` is the startup boundary; `src/ui/router.js` reserves route constants; `src/state/` contains the metadata-only storage boundary.
