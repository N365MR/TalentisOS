# Coding Standards

Use ES modules, semantic HTML, centralized CSS tokens, small explicit modules, and Node-native tests. Avoid dependencies unless they provide clear foundation value. Do not introduce product-domain state or UI before the phase that authorizes it.
