{
  "name": "talentisos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "node --test",
    "check": "node --check src/main.js && node --check src/state/schema.js && node --check src/state/storage.js && node --check src/ui/router.js && node --check src/ui/render.js && node --check public/sw.js && node --check vite.config.js"
  },
  "devDependencies": {
    "vite": "^7.1.7"
  }
}
